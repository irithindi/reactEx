const string = 'I love React!'

const String=()=>{
    // return (<div>{'The string’s length is:'+ string.length}</div>)
    return (<div>{`The string’s length is: ${string.length}`}</div>)
}

export default String